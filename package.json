{
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env build.js --dev",
    "build": "node --env-file=.env build.js"
  },
  "engines": {
    "node": "22.x",
    "pnpm": "9.x"
  },
  "dependencies": {
    "esbuild": "^0.24.0",
    "navaid": "^1.2.0",
    "umai": "^0.4.0"
  },
  "devDependencies": {
    "servbot": "^0.2.6"
  }
}
